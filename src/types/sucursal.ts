export type PropSucursal = {
  id: number;
  nombre: string;
  direccion: string;
  serie_factura: string;
  numero_factura_inicial: number;
  serie_boleta: string;
  numero_boleta_inicial: number;
  codigo_anexo: string;
  fecha_creacion: string;
}